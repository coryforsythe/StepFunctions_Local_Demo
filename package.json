{
  "name": "sam-app",
  "version": "1.0.0",
  "devDependencies": {
    "grunt": "^1.0.4",
    "grunt-run": "^0.8.1"
  },
  "scripts": {
    "lambda":"sam local start-lambda --region us-east-1 -p 9001",
    "test": "grunt test"
  }
}
